spec:
  template:
    spec:
      containers:
      - name: postgresql
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secrets-new
              key: DB_PASSWORD
        - name: POSTGRES_DB
          value: globepay_prod
        - name: POSTGRES_USER
          value: globepay
