apiVersion: batch/v1
kind: Job
metadata:
  name: database-migration
  annotations:
    # This annotation forces recreation of the job on each sync
    argocd.argoproj.io/sync-options: Replace=true
    # This annotation indicates that the resource should be replaced instead of applied
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  template:
    metadata:
      annotations:
        # This annotation forces recreation of the job on each sync
        argocd.argoproj.io/sync-options: Replace=true
    spec:
      containers:
      - name: migrate
        image: 907849381252.dkr.ecr.us-east-1.amazonaws.com/globepay-backend:latest
        command: ["/app/migrate"]
        args: ["up"]
        env:
        - name: DATABASE_URL
          value: "postgresql://globepay:$(DB_PASSWORD)@postgresql.globepay-prod.svc.cluster.local:5432/globepay_prod?sslmode=disable"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: backend-secrets
              key: DB_PASSWORD
      restartPolicy: Never
  backoffLimit: 4